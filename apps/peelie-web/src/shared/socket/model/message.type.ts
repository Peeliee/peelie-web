// TODO: socket message payload DTO 정의하기

export interface MessageSendDTO {
  roomId: string;
  // clientMessageId: string;
  // text: string;
}

export interface MessageNewDTO {
  id: string;
  // roomId: string;
  // text: string;
  // senderId: string;
  // createdAt: string;
}
